rule "Wake up run Spotify"
when
	Item homeSleep received command OFF
then
	if(denon_power.state == OFF){
        // while(denon_power.state != ON){
        // Thread::sleep(12000) 
        // }
        spotifyDevices.sendCommand("d57aa2c86b92392567aa3ebc8340813a076bff43")
        spotifyPlaylists.sendCommand("spotify:playlist:37i9dQZF1E380X0aiT8MiT")
        spotifyTrackPlayer.sendCommand(PLAY)
         Thread::sleep(12000)
        spotifyDeviceVolume.sendCommand(35)
        // spotifyPlaylists.sendCommand("spotify:playlist:5rBaq7k3Y7YnpFi3snLqyk")        
        // spotifyDeviceVolume.sendCommand(35)        
    }
    logInfo("Spotify", "Wake up playing")

end